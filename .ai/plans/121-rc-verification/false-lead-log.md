# False-Lead Log — RC-сессия 121 (ночь 2026-08-03)

<!-- GREP_SUMMARY: false-lead-log, rc-verification, 121, misleading, drift, recommendations -->
# region MODULE_CONTRACT
## @purpose  Журнал ситуаций, которые ввели в заблуждение или пустили по ложному следу во время ночной RC-сессии 121. Цель — архитектор анализирует лог, закрывает дрейфы, проводит рефакторинг.
## @scope    Все фазы RC-сессии: локальный стек, e2e, прод-бустрап, CI/CD, долги.
## @invariants
##   1. Каждая запись — наблюдаемый факт, а не интерпретация
##   2. «Что оказалось на самом деле» — проверено кодом/командой, не по памяти
##   3. Рекомендация — конкретное действие для архитектора
# endregion MODULE_CONTRACT

| # | Время (UTC+3) | Что ввело в заблуждение | Что оказалось на самом деле | Как разрешилось | Рекомендация архитектору |
|---|---------------|-------------------------|-----------------------------|------------------|--------------------------|
| 1 | 02:25 | NGINX_CERT_DIR=./dev-certs монтируется в <repo-root>/dev-certs (анализ платформенных env) | docker compose резолвит относительные bind-пути include'ных файлов ОТНОСИТЕЛЬНО директории include'ного файла: mount source = core/modules/nginx/dev-certs | Проверено docker inspect nginx Mounts; DEV_CERTS_LIVE_ROOT перенаправлен в модуль dev-certs | Документировать правило резолвинга относительных путей compose include (или везде absolute paths) |
| 2 | 02:33 | status-page unhealthy: status-metrics.json «не создаётся» | /run/platform/status-metrics.json на хосте — ДИРЕКТОРИЯ (Docker сам создал mount source); /run на macOS read-only для пользователя | Параметризован STATUS_METRICS_JSON (compose + exporter), .env → <repo>/.local/ | Проверить ВСЕ compose-маунты /run/platform и /opt/... на dev-локали (mapping-документ) |
| 3 | 02:50 | «postgres пароль сходится с .env» — psql -h 127.0.0.1 с PGPASSWORD=$POSTGRES_PASSWORD вернул 1 | pg_hba.conf: 127.0.0.1/32 = **trust** — проверка пароля вообще не выполнялась; реальный пароль volume ≠ test-pg-pwd (pgbouncer md5-аутентификация с 172.x падала) | ALTER USER postgres PASSWORD (синхронизация локальных данных с .env) | Для локальных БД-проверок всегда тестировать через сетевой путь (не loopback-trust) |
| 4 | 03:00 | /etc/hosts можно обновить через osascript (GUI-пароль администратора) | Ночная сессия без GUI-ввода — osascript висел до таймаута (2 попытки) | BLOCKED: curl --resolve для всех проверок; команда для оператора в отчёте | Ночным сессиям нужен заранее настроенный sudoers/пароль или документированный обход |
| 5 | 03:10 | detect-age-key вернул ключ из «env» — показалось, что AGE_SECRET_KEY_FILE прочитан | В интерактивном shell оказался экспортирован AGE_SECRET_KEY (утёк в лог сессии первым 8-символьным префиксом, полный ключ НЕ выводился) | Вывод маскирован; фикс не требовался; в bootstrap-фоне env чистый | node_detect --detect-age-key при env-наличии печатает источник; не запускать в шеллах с экспортированным AGE_SECRET_KEY |
| 6 | 03:20 | make bootstrap-node NODE=test-e2e с AGE_SECRET_KEY_FILE=... — «file not found: /Users/.../age-key-personal.txt» на VPS | Make-таргет передаёт AGE_SECRET_KEY_FILE как CLI-аргумент; bootstrap.sh не обрабатывал его → путь уходил в remote passthrough (node-lifecycle --age-secret-key-file <локальный путь> на VPS) | Фикс bootstrap.sh: --age-secret-key-file читается локально через node_detect-цепочку (как node-update.sh); DRY_RUN-верификация | Правило: remote-команды никогда не получают локальные пути — добавить гейт/тест |
| 7 | 03:30 | φ1 bootstrap: pip не может поставить requirements — «Cannot uninstall jsonschema 4.10.3 (installed by debian, no RECORD)» | φ1 ставит apt python3-jsonschema (4.10.3, без RECORD); requirements требует >=4.17 → pip uninstall fail | python_deps: jsonschema --ignore-installed (паттерн typing_extensions); повторный bootstrap | Апт-пакеты φ1 и pip-requirements не должны конфликтовать — единый перечень python-зависимостей |
| 8 | 03:45 | PLATFORM_ROOT=<локальный путь> уходит в remote-команды (build_ssh_cmd remote_root = PLATFORM_REMOTE_BASE → PLATFORM_ROOT → /opt/platform) | e2e 095 «работал» потому, что core_deliverer доставлял core в ТОТ ЖЕ локальный путь на VPS (/Users/tronyx/projects/ai-platform/) — двойное дерево и мусор на VPS; канон закреплён тестом test_platform_remote_base_chain | НЕ чинил (риск регрессий, тесты завязаны); зафиксировано в Backlog; после bootstrap очистить /Users/... на VPS | Пересмотреть канон: remote-root НЕ должен наследовать локальный PLATFORM_ROOT; разделить local/remote env |
| 9 | 04:10 | gate упал на yamllint, но ручной `pre-commit run yamllint` — Passed | Реальный фейл был ruff-check (I001 import unsorted в nginx_harness после C6); вывод pre-commit скрывал детали (показывал только последний Failed-хук в summary) | ruff --fix + format, повторный прогон | pre-commit-run summary показывает не все Failed-хуки — выводить полный список |
| 10 | 04:40 | docker compose ps показывает «nginx healthy», но platform /health → 503 | status-page /health — агрегированный вердикт: 8 exited hermes-test-l1/l2-* контейнеров от старых pytest-сессий (не платформа) | docker rm hermes-test-* + регенерация status-metrics.json → /health 200 | CI/pytest должны чистить test-контейнеры после сессий (conftest cleanup) |
| 11 | 06:10 | «nginx healthy, prod-сайты 000» — казалось, vhost'ы не сгенерированы | 000 был из-за отсутствия vhost'а tronyx.ru: NGINX_OVERLAY_DIR на проде не устанавливался (sequential-деплой не прокидывал overlay; контекст пуст — нет contexts[]); default overlay = core/modules/nginx/overlays | config_overlay fallback + sequential-overlay прокидывание; nginx пересоздан → vhost'ы загружены (502 = upstream'ы проектов не развёрнуты) | Дрейф: sequential vs parallel overlay-прокидывание — единый контракт |
| 12 | 06:15 | Локальные dev-vhost'ы *.local «исчезли» из core/modules/nginx/overlays | Их унёс rsync core/ на VPS (untracked, вне gitignore) — там они стали overlay'ем и ломали прод-nginx (ai-platform.local cert) | Перенос в .local/overlays/nginx + NGINX_OVERLAY_DIR в .env; на VPS — --delete core-rsync очистил | Локальные артефакты не должны жить в core/ (в т.ч. untracked) |
| 13 | 06:35 | «tor.enabled: true не ставит tor» — детект «работает» вручную | node_yaml CLI возвращает Python-bool `True` (не YAML-`true`); сравнение `== "true"` не совпадает | case-insensitive сравнение в detect_tor_enabled | Булевы из node_yaml CLI — нормализовать к lowercase |
