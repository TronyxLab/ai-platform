# GREP_SUMMARY: compile-config, framework, version, targets, model, runtime, variables
# STRUCTURE: ┌compile config┐ → ◇ parse targets → ◇ set model routing → ◇ configure runtime → ⊕ generate
# region MODULE_CONTRACT
## @purpose  Compile-time configuration for the framework compiler — targets, model routing, runtime features, and template variables
## @scope    Framework compilation configuration; auto-generated by compiler
## @invariants auto-generated; do not edit manually
# endregion MODULE_CONTRACT

framework:
  version: "0.6.1"

targets:
  - kilo

model:
  pro: "deepseek/deepseek-v4-pro"
  flash: "deepseek/deepseek-v4-flash"

runtime:
  shell: "/bin/zsh"
  features:
    thinking_mode: true
    background_process: true

variables:
  project_name: "AI-first development platform"
  stack: "Bash + Python + Docker Compose + GitHub Actions + SOPS/Age + LiteLLM + Prometheus + Grafana + Loki + Langfuse + Postgresql + Redis"
  description: "Platform for ai first web development"
  language: ru  # Agents will reply in Russian
