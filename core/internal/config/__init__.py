# GREP_SUMMARY: config, platform-config, init
# STRUCTURE: ── empty init for config package ──
# region MODULE_CONTRACT
## @purpose  Package init for core/internal/config — enables import from core.internal.config.platform_config
## @scope    Empty init file; all functionality provided by sibling modules
## @invariants  No code — just package marker
# endregion MODULE_CONTRACT
