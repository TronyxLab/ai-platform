# GREP_SUMMARY: profile default general-purpose assistant blank-slate
# STRUCTURE: ▶ profile:default → model:0.7/4096 → telemetry:INFO → ⎋
# region MODULE_CONTRACT
## @purpose     Default general-purpose assistant profile — Blank Slate
## @scope       Inherits base config; adds agent personality, model params
## @invariants
##   - model.temperature: 0.7 — balanced creativity/precision
##   - model.max_tokens: 4096 — standard context window
## @rationale   Blank Slate profile: carries only personality-defining params.
##              Add tools, browser, web search, MCPs manually as needed.
## @changes     LAST_CHANGE: 2026-06-28 | Blank Slate migration
# endregion MODULE_CONTRACT

profile:
  name: default
  description: General-purpose assistant profile for AI Platform

model:
  temperature: 0.7
  max_tokens: 4096

telemetry:
  log_level: INFO
