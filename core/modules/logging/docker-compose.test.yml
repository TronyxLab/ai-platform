# GREP_SUMMARY: logging docker-compose test override container_name-test loki test-observability-net
# STRUCTURE: base.yml + test.yml → loki with -test suffix and shifted port
# region MODULE_CONTRACT
## @purpose  Test override for logging — -test container_name for loki (DevPlan 010 T3.1: alloy удалён → log-collector)
## @scope    Only used in test fixtures. Never used in production.
## @invariants
##   - container_name: loki-test
##   - restart: "no" для всех контейнеров (тестовые контейнеры не auto-рестартуются)
##   - Loki port: 127.0.0.1:13100:3100
##   - Alloy-test перенесён в core/modules/log-collector/docker-compose.test.yml (010 T3.1)
## @rationale Стандартный test-overlay контракт (DevPlan 04 DD2)
## @changes 2026-08-22 | DevPlan 010 T3.1 — alloy-test удалён (→ log-collector test-overlay)
# endregion MODULE_CONTRACT

services:
  loki:
    container_name: loki-test
    networks: !override  # DevPlan 017 W2.5 — test network isolation
      - test-observability-net
    ports: !override  # ⚠️ TRAP[BUG] · 2026-07-18 · HIGH · compose merge склеивает ports base+test → тестовый loki биндит 3100 (F-7) · Fix: !override
      - "127.0.0.1:13100:3100"
    restart: "no"

networks:
  test-observability-net:
    external: true
