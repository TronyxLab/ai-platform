# 🧐 TRAP[DECISION] · 2026-07-03 · — · pg_hba.test.conf: trust для всех тестовых подключений
# · Rejected: md5/scram-sha-256 (production)
# · Reason: Тесты pgbouncer используют AUTH_TYPE=trust; pgbouncer→postgres должен
# ·   проходить без пароля. В production используется pg_hba.conf с md5/scram.
# · Rev: если тесты перейдут на SCRAM-аутентификацию — удалить этот файл
# GREP_SUMMARY: pg_hba test trust all-connections localhost docker-network
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust
host    all             all             172.16.0.0/12           trust
host    all             all             10.0.0.0/8              trust
host    all             all             192.168.0.0/16          trust
host    all             all             all                     trust
