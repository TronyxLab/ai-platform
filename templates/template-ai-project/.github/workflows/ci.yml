# GENERATED by template-ai-project — ручные правки затираются практиками
# GREP_SUMMARY: GitHub Actions CI template-ai-project typecheck test lint self-check gates
# STRUCTURE: on(push/PR) → jobs.quality(install → typecheck → test → lint → self-check)
# region MODULE_CONTRACT
## @purpose  CI проекта-бота (template-ai-project, W3 D9; бриф §6.4): quality-цепочка
##           typecheck → test → lint → self-check (гейты @ai-project/static, os-gates.json).
## @scope    Каждый push/PR в проект; деплой — отдельный workflow (deploy-project.yml платформы,
##           как в template-frontend/.github/workflows/deploy.yml — org-agnostic {{ORG_NAME}}).
## @invariants
##   - Порядок: typecheck → test → lint → self-check (сначала типы, потом поведение, потом стиль)
##   - self-check — последний: гейты структурные, падают на всём скоупе
##   - @ai-project/* пакеты тянутся из GHCR npm (registry в package.json, publishConfig)
## @rationale Бриф §6.4: «Новый проект с cordis-плагинами получает эти проверки из коробки;
##   нарушение ломает гейт, а не ревью».
# endregion MODULE_CONTRACT

name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://npm.pkg.github.com
      - name: Install
        run: npm ci
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Typecheck
        run: npm run typecheck
      - name: Test
        run: npm run test
      - name: Lint
        run: npm run lint
      - name: Self-check (gates)
        run: npm run self-check
