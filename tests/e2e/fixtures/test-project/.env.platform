# GREP_SUMMARY: fixture test-project env-platform minimal E2E
# @purpose Canonical test-project env (DevPlan 095 T18) — minimal deterministic set.
#   Sourced by the deploy pipeline into the project container environment.
# @invariants
#   - ENV=test marks this as a non-production fixture (never deployed to real nodes)
ENV=test
