# @purpose Test node.yaml fixture for predeploy gate local testing — mirrors production node.yaml with a test project
# Test node.yaml for predeploy gate local testing
# Mirrors real node.yaml structure but with a test project
# Used by tests/_conftest/predeploy.py fixture when no production node.yaml is available

node:
  name: test-node
  host: 127.0.0.1
  timezone: UTC

domain: test.example.com
email: test@example.com

projects:
  - name: test-site
    domain: test-site.example.com
    repo: https://github.com/example/test-site.git
    branch: main
